// Optional local Places browser key (same key you’d put in the meta tag).
// 1. Copy this file to `config.local.js` in the repo root (next to index.html).
// 2. Paste your key into the string below. `config.local.js` is gitignored.
// Use when the HTML meta tag is still empty (e.g. Live Preview on index.html
// without `npm run dev`). Prefer `npm run dev` + `.env` GOOGLE_PLACES_API_KEY
// or production inject into the meta tag when you can.
window.__TOP100_GOOGLE_PLACES_KEY__ = '';
